local_map_builder:
  ros__parameters:
    resolution: 0.5
    width: 40
    height: 40

local_planner:
  ros__parameters:
    forward_speed: 0.5

safety_monitor:
  ros__parameters:
    min_points_threshold: 10
